# Decision Log — aictl PRD

Canonical memory and audit trail. Every decision, change, and override is recorded here.

## 2026-06-15 — Session start

- **Intent:** Create (no existing PRD found in `_bmad-output/planning-artifacts/prds/`).
- **Input:** `BRIEF.md` (project root) — detailed brain dump / design exploration for `aictl`, a provider-agnostic supervisor for agentic coding CLIs.
- **Stakes calibration:** Open-source dev tool (public release; adoption/extensibility matters; not enterprise rigor).
- **Working mode:** Fast path (batch gaps → draft with `[ASSUMPTION]` tags → iterate).

### Calibration decisions (user-confirmed via consolidated questions)

1. **v1 scope = Brief's MVP.** PTY runner (real provider UI) + `init`/`start`/`note`/`checkpoint`/`handoff` + deterministic handoff generation + usage-limit detection + `run --fallback` chain.
2. **Platforms = macOS + Linux.** Unix PTY (`creack/pty`). Windows (ConPTY) explicitly deferred.
3. **Providers = Trio + config-driven generic.** Claude Code, Codex, Gemini ship tested; plus a generic config-driven adapter (command/args/inject-mode/usage-patterns) for any CLI.
4. **Handoff generation = Deterministic template only.** No LLM dependency in v1 — must work even when all providers are exhausted. LLM summarize/compaction deferred to post-v1.

### Naming
- **Name settled: `aictl`.** Confirmed by project dir + `_bmad` config `project_name: aictl`. Brief floated alternatives (agentmux, relay, codemux); not chosen. [Recorded — not re-litigated unless user reopens.]

### Draft review — load-bearing calls resolved (user-confirmed)

- **`recover` in v1 MVP: YES.** FR-14 / UJ-3 kept; assumption tags removed; removed from Open Questions.
- **Session concurrency: one active Session per repo working tree** (second start refused). Glossary updated; removed from Open Questions.
- **Distribution: `go install` + prebuilt macOS/Linux binaries on GitHub releases.** Homebrew deferred. Added to Developer-Product Concerns; removed from Open Questions.
- **Ctrl-C (`user_cancel`) mid-fallback: stop entirely**, do not advance. Added to FR-20 consequences; removed from Open Questions.
- **FR-20 non-usage-failure-stops-chain** and **FR-21 evidence-only handoff**: confirmed as defaults (assumption tags removed).
- **Secret handling (v1, maintainer default, not separately asked):** git-tracked-only diffs + configurable path denylist; deeper content scanning deferred. Recorded as `[ASSUMPTION: denylist defaults TBD]`.

Open Questions remaining (genuine unknowns, left for research): per-provider prompt-injection reliability, usage-limit pattern maintenance/false-positives, "files changed" granularity (git-only vs untracked).

### Finalize (2026-06-15)

- **Decision-log audit:** all decisions reflected in PRD/addendum; no orphans.
- **Input reconciliation** (`reconcile-brief.md`): high fidelity vs BRIEF.md. One real gap fixed (command-log population scope, §6.2). Two deliberate simplifications noted (`switch` verb folded into `run`; `--handoff` flag made implicit).
- **Reviewer pass** (`review-rubric.md`): grade **Good**. No critical/high. Autofixed: FR-9 broken cross-ref (FR-15→FR-20); perf NFR adjectives → rough ~1s bound (NFR-6 + Performance budgets); SM-2 anchored to fidelity checklist; Assumptions Index roundtrip completed (added telemetry, session-dir public surface, semver, Go runtime, why-now).
- **Open-item triage:** 3 remaining Open Questions (prompt-injection reliability, usage-pattern maintenance, files-changed granularity) — **deferred, none phase-blocking**. Owner: maintainer (Daniel). Revisit condition: during architecture / first provider-integration spike. Remaining `[ASSUMPTION]` tags are minor inferences; `[NOTE FOR PM]` on `summarize` revisits when long sessions strain handoff size.
- **Polish:** consolidated editorial pass applied inline (structure-then-prose aligned to doc_standards); document already clean, heavyweight editorial skills not separately invoked.
- **External handoffs:** none configured (`external_handoffs` empty).
- **Close:** `prd.md` `status: final`, `updated: 2026-06-15`.

### Captured to addendum during drafting
- Technical "how" from the brief (Go internals, `creack/pty` snippets, MultiWriter/TeeReader, PTY runner shape, package layout, interface signatures, prompt-injection mechanics, bracketed paste). These are implementation-level and belong in architecture/solution-design, not the PRD.
